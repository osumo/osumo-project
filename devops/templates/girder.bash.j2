#! /usr/bin/env bash

cd "$( dirname "$0" )"

source scripts/env

pushd girder
/opt/nodejs/v5.4.0/bin/npm install
python -m girder &

if [ '!' -d initialized ] ; then
    # mkdir initialized
    true
fi

popd

wait

