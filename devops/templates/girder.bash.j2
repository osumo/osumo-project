#! /usr/bin/env bash

cd "$( dirname "$0" )"

rm -rf cache/env
source scripts/env

pushd girder
npm install -g grunt-cli
npm install
python -m girder &

if [ '!' -d initialized ] ; then
    # mkdir initialized
    true
fi

popd

wait

